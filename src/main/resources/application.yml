spring:
  application:
    name: todolist

  datasource:
    url: jdbc:mysql://localhost:3306/pookie_database
    username: root
    password: Supdeb@1614
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect
